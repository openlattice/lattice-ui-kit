/*
 * @flow
 */

import styled from 'styled-components';
import type { ReactComponentStyled } from 'styled-components';

const OverlayInnerContainer :ReactComponentStyled<*> = styled.div`
  position: relative;
`;

export default OverlayInnerContainer;
