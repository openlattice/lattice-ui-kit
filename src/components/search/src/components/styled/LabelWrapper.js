import styled from 'styled-components';

const LabelWrapper = styled.div`
  display: flex;
  flex: 1 0 auto;
  flex-direction: row;
  align-items: center;
  white-space: nowrap;
`;

export default LabelWrapper;
