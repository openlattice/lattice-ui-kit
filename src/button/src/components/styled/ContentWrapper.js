import styled from 'styled-components';

const ContentWrapper = styled.span`
  align-self: center;
  display: inline-flex;
  flex-wrap: nowrap;
  max-width: 100%;
  position: relative;
  overflow: hidden;
  min-width: 14px;
`;

export default ContentWrapper;
